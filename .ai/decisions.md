# Decisions

- The GitHub profile repository is the top-level AI bootstrap through `AI.md`; canonical workflow logic remains in `syllik/ai-workflow`.
- `workspace.yaml` in `syllik/ai-workflow` remains the canonical machine project registry.
- Profile workspace documentation is human-facing navigation and must stay synchronized with canonical registry changes.
