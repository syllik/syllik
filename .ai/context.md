# syllik profile context

- Repository: `syllik/syllik`.
- Purpose: public profile plus human-facing workspace navigation and repository documentation.
- Integration branch: `master`.
- Canonical AI lifecycle, roles, project registry, and execution policy belong to `syllik/ai-workflow`.
- `AI.md` is the machine-facing bootstrap from the profile repository into the canonical workflow.
- `docs/workspace.md` describes the local workspace tree; `docs/repositories.md` records human-facing repository metadata.
- Do not duplicate the canonical AI lifecycle or full workspace manifest in the profile README.
- Changes here are documentation/profile changes unless an explicit task says otherwise.
